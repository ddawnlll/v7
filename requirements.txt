# v7 lab dependencies.
#
# Exact versions frozen from the remote box (Linux, CPython 3.12.3) during
# Phase 0/1 lock verification. A clean checkout must reproduce this environment:
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements.txt && python -m pytest lab/tests/ -q

# Runtime — imported by lab/indicators.py
numpy==2.5.1

# Runtime — parquet I/O for tools/data.py and tools/download_binance.py.
# lab/ never imports pyarrow: the deterministic core hashes canonical text.
# never parquet container bytes (not writer-stable). Only the acquisition tool
# touches pyarrow.
pyarrow==25.0.0

# Test runner — required to run lab/tests/
pytest==9.1.1

# Transitive (pinned for reproducibility; pulled in by pytest)
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0

# Mutation audit (RULES §14/§16 verification, cosmic-ray.toml) — not imported by
# lab/ or tools/ at runtime, only needed to run the sim.py mutation audit.
cosmic-ray==8.4.6
